task: "Close all 8 remaining capability gaps in Slice of Pi: session fork, prompt templates, skill injection, extension toggle, config viewer, web scrape/image tools, full tool results, session naming + compaction config. Achieve 100% parity with pi native capabilities."

started: 2026-06-09T22:08:00-04:00
git_baseline: 06d897163831a6409fad8f43aae89fd396bceed5
status: before

## Phase 1 — Task Spec
**Clarified task**: Build Slice of Pi — a web dashboard + API server that manages multiple pi coding agents using the design from agent-fleet-console.html, implemented across 18 verifiable steps in 4 phases.

**In scope**:
- Pi Orchestrator API (FastAPI, 8 routers, 4 services)
- SQLite database (6 tables)
- WebSocket event bus (in-process)
- SSE chat streaming
- Pi MCP Server (10 tools, STDIO + HTTP/SSE)
- Vue 3 Dashboard (8 views, ~25 components)
- Pi Scheduler (APScheduler, no Redis)
- Agent persona system (read existing .pi/agents/*.md)

**Out of scope**:
- Docker containers (agents ARE pi sessions)
- Redis (single-user)
- Multi-user auth, JWT
- Slack/Telegram/WhatsApp
- coms/coms-net implementation (already exists in pi)

**Assumptions confirmed**:
- pi SDK accessed via `pi --mode json` subprocess spawning (NOT direct import — pi package is ESM-only)
- Chat streaming: SSE (POST /api/agents/:id/chat returns text/event-stream)
- Dashboard events: WebSocket (GET /ws/events)
- Ports: 8420 (orchestrator), 5173 (Vite dev)
- pi binary at ~/.nvm/versions/node/v22.22.2/bin/pi (v0.79.0)

**Gate**: PASSED (auto)

## Phase 2 — Execution Plan

### Item 1: E2E pipeline test
1. 1a: Create agent via API with real pi tools → verify: POST /api/agents returns 201 with status "idle"
2. 1b: Send chat via SSE → verify: curl -N POST /api/agents/:id/chat streams text/event-stream with chunks
3. 1c: Verify JSONL session file → verify: session file at ~/.pi/agent/sessions/managed/ has >0 lines

### Item 2: Fill stub views
4. 2a: Agents.vue — searchable grid with status badges, create/delete → verify: page shows agents from API with colored status dots
5. 2b: Sessions.vue — scrollable history with expandable messages → verify: page lists sessions with message preview

### Item 3: Wire chat tab
6. 3a: ChatPanel.vue — SSE reader, message list, input → verify: component renders message list and input
7. 3b: Wire into AgentDetail overlay Chat tab → verify: type message, see streaming response in chat bubbles

### Item 4: coms integration
8. 4a: GET /api/coms endpoint → verify: curl returns peer list from ~/.pi/coms/projects/
9. 4b: ComsPanel.vue — peer pool list → verify: panel shows agent names, models
10. 4c: Add ComsPanel to Dashboard → verify: dashboard bottom row includes coms panel

### Item 5: Maestro integration
11. 5a: GET /api/teams endpoint → verify: curl returns teams from ~/.pi/agents/teams.yaml
12. 5b: POST /api/teams/:name/deploy → verify: deploys all agents in team, returns created agent IDs
13. 5c: Teams.vue — roster cards with deploy → verify: page shows teams, deploy button creates agents

### Plan Integrity
- Total steps: 13
- Unbounded verify conditions: none
- Gate: PASSED
- Dependencies: Item 1 must pass before 2-5 (need working pipeline). Items 2-5 are independent of each other.

Full plan in `IMPLEMENTATION_PLAN.md`. Summary:

### Phase A — Backend Foundation (6 steps)
1. A1: Project scaffold + config → verify: `python -c "from pi_orchestrator.config import DATABASE_PATH; print(DATABASE_PATH)"` prints valid path under ~/.pi/
2. A2: Database schema + models → verify: SQL query confirms 6 tables exist (agents, sessions, activities, schedules, schedule_executions, settings)
3. A3: Pi session service → verify: `hasattr(PiSessionService, 'create_session')` etc. — all 6 methods callable
4. A4: Event bus + WebSocket → verify: wscat connects to ws://localhost:8420/ws/events, published event arrives within 200ms
5. A5: main.py + FastAPI lifespan → verify: `curl http://localhost:8420/health` → `{"status":"ok"}`
6. A6: Agent CRUD router → verify: POST /api/agents → 201, GET /api/agents → includes new agent

### Phase B — Core Features (3 steps)
7. B7: Chat router (streaming) → verify: curl POST /api/agents/:id/chat returns SSE stream with agent response
8. B8: Session history router → verify: curl GET /api/sessions returns JSON array with completed sessions
9. B9: Activity tracking service → verify: sqlite3 SELECT count(*) FROM activities returns > 0 after chat

### Phase C — Dashboard UI (5 steps)
10. C10: Vue 3 project with design system → verify: npm run dev shows dark background + indigo accent + correct fonts
11. C11: Layout shell (nav + sidebar + main) → verify: responsive at 768px, sidebar hides
12. C12: Dashboard view (stats + grid + activity + ops) → verify: all components render with mock data
13. C13: Agent detail overlay → verify: click card opens overlay, Escape closes
14. C14: Wire dashboard to live API → verify: create agent via API → appears in grid within 2s via WebSocket

### Phase D — Advanced Features (4 steps)
15. D15: Skills + Extensions catalog → verify: curl GET /api/skills returns JSON with name/description/triggers
16. D16: Scheduler service + router + UI → verify: create schedule via API → appears in list; UI form works
17. D17: MCP server (10 tools) → verify: MCP Inspector lists 10 pi tools
18. D18: Template gallery → verify: curl GET /api/templates returns persona list

### Plan Integrity
- Total steps: 18 (in 4 phases — Phase A first)
- Unbounded steps: none
- All verify conditions: runnable, binary, specific
- Gate: PASSED

### Execution Strategy
Each phase is a checkpoint. After completing a phase, confirm results before advancing. Phases are ordered by dependency: A→B→C→D. Within a phase, steps are strictly ordered by dependency.
